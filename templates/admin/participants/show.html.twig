{% extends 'base.html.twig' %}

{% block title %}Participants
{% endblock %}

{% block body %}
	<section class="container">
		<div class="row">
			<div class="col">
				<h1>Participants</h1>

				<table class="table">
					<tbody>
						<tr>
							<th>Id</th>
							<td>{{ participant.id }}</td>
						</tr>
						<tr>
							<th>Name</th>
							<td>{{ participant.user.Name }}</td>
						</tr>
						<tr>
							<th>Email</th>
							<td>{{ participant.user.email }}</td>
						</tr>
						<tr>
							<th>Telephone</th>
							<td>{{ participant.user.telephone }}</td>
						</tr>
						<tr>
							<th>Evénement</th>
							<td>{{ participant.event.name }}</td>
						</tr>
						<tr>
							<th>Created_at</th>
							<td>{{ participant.createdAt ? participant.createdAt|date('Y-m-d H:i:s') : '' }}</td>
						</tr>
					</tbody>
				</table>

				<a href="{{ path('admin_participants_index') }}">back to list</a>

				<a href="{{ path('admin_participants_edit', {'id': participant.id}) }}">edit</a>
			</div>
		</div>
	</section>

	{{ include('admin/participants/_delete_form.html.twig') }}
{% endblock %}
