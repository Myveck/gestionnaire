<form method="post" action="{{ path('admin_participants_delete', {'id': participant.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer ce participant ?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ participant.id) }}">
	<button class="btn btn-danger">Supprimer</button>
</form>
