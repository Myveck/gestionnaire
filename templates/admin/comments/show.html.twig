{% extends 'base.html.twig' %}

{% block title %}Commentaires
{% endblock %}

{% block body %}
	<main class="container">
		<section class="row">
			<div class="col">
				<h1>Commentaires</h1>

				<table class="table">
					<tbody>
						<tr>
							<th>Message</th>
							<td>{{ comment.message }}</td>
						</tr>
						<tr>
							<th>Date</th>
							<td>{{ comment.commentDate ? comment.commentDate|date('Y-m-d H:i:s') : '' }}</td>
						</tr>
						<tr>
							<th>Evénement</th>
							<td>{{ comment.event.name }}</td>
						</tr>
					</tbody>
				</table>

				<a href="{{ path('admin_comments_index') }}">retour vers la liste</a>

				<a href="{{ path('admin_comments_edit', {'id': comment.id}) }}">modifier</a>

				{{ include('admin/comments/_delete_form.html.twig') }}
			</div>
		</section>
	</main>
{% endblock %}
